[Unit]
Description=Restart ollama-router when the TLS certificate is renewed (path = control_tls.cert in config.yaml)

[Path]
PathChanged=/etc/ollama-router/certs/fullchain.pem
Unit=ollama-router-cert.service

[Install]
WantedBy=multi-user.target
