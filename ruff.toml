# Lint fuer den Router (2026-09-16, erweitert 2026-09-25): echte Fehler, Bugbear, blinde except-Klauseln, haengende asyncio-Tasks.
#   python -m ruff check router/ollama_router
line-length = 150
target-version = "py311"   # Debian 12 auf dem CT

[lint]
select = ["F", "E9", "B", "BLE", "RUF006"]   # 2026-09-25: ganz Bugbear, blinde except (noqa BLE001 ist begruendet), haengende Tasks
